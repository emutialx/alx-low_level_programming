#include "main.h"

/**
 *  * _abs - computes the absolute value of an integer
 * @ab: the value to be computed
 *
 * Return: 0 0r 1 _abs
 *
 */

int _abs(int ab)

{

	return (ab * ((ab > 0) - (ab < 0)));

}
